# Changelog

本文件记录 `XunYu` 的重要变更。

当前仓库尚未发布正式版本；因此先以 `Unreleased` 维护主干变更。

## [Unreleased]

### Added

- 新增 `CONTRIBUTING.md`，统一本地开发、验证与提交协作约定。
- 新增 `CHANGELOG.md`，为后续版本化发布保留连续变更记录。

### Changed

- 统一 GitHub 仓库、`README.md` 与包元数据中的项目命名口径为 `XunYu`。
- 统一许可证声明口径为 `AGPL-3.0-or-later`。
- 更新仓库克隆示例与本地目录入口到 `https://github.com/jinghu-moon/XunYu.git` / `XunYu`。
